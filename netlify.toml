[build]
  functions = "netlify/functions"
  command = "echo 'Static site with serverless functions'"
  publish = "."

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_ENV = "production"
